# Word Count

What it says on the tin. Displays a small widget that calculates a chat's word count. Click it to cycle between word and token counts. A very small extension for a very small task :)

## Usage

- The widget appears in the bottom-right corner of your chat.
- **Click** to cycle between word count and token count.
- Use `/wordcount` or `/wc` to toggle visibility.
